#include"utils.h"

int main() {
    Timer timer;

    long long int num = 600851475143;
    //sqrt(600851475143) ~= 775146
    for(long long int i = 1; i * i <= num; ++i) {
        if()
    } 

    timer.elapsed();
    return 0;
}